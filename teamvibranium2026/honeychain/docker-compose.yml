services:
  db:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: honey
      POSTGRES_PASSWORD: chain2026
      POSTGRES_DB: honeychain
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data
volumes:
  pgdata:
